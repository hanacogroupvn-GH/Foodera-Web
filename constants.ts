
import { Product, NewsItem } from './types';

// Hard-coded fallback data has been removed.
// All product and news content is now managed exclusively through the backend CMS.
// These empty arrays serve as safe defaults when the backend is unavailable.

export const PRODUCTS: Product[] = [];

export const NEWS: NewsItem[] = [];
