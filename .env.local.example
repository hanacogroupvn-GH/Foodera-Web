# Local overrides for this machine only.
# This file should stay uncommitted as `.env.local`.

# Force local SQLite while keeping official Turso config in `.env` or hosting env.
DATABASE_MODE=local
LOCAL_DATABASE_PATH=tmp/foodmax-local.db

# Optional local-only overrides
# PORT=8787
# AI_PROVIDER=ollama
